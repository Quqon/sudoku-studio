# Memory

- 스도쿠는 3D 이동 게임이 아니라 정밀한 2D 보드 인터랙션이 핵심이다. Babylon 씬은 직교 카메라와 동적 텍스처 드로잉으로 구현하고, 포인터 좌표를 보드 좌표로 변환한다.
- React는 캔버스의 수명주기만 담당하며, 퍼즐 상태·입력·드로잉은 `client/src/game/`의 프레임워크 독립 클래스에 둔다.
- 생성 자산은 프로젝트 디렉터리에 복사하지 않고 `/manus-storage/...` URL을 텍스처로 사용한다.
- 브라우저 검증에서 1280×1100(비율 1.164) 환경은 의도대로 세로형 도구 트레이를 사용했다. 1440×900 같은 가로형 화면에서는 보드 우측 마진 노트가 나와야 하므로, 다음 시각 검증에서 두 비율을 모두 확인한다.
- 데모 화면에서 캔버스 포인터 선택과 `1` 키 입력 이벤트를 전달했다. 게임은 DOM 폼이 아닌 Babylon 캔버스이므로, 이후 시각 확인으로 입력 숫자와 선택 강조의 반영을 최종 점검한다.
- 힌트 개선 후 데모 퍼즐에서 선택된 빈칸을 유지한 채 `H` 키 이벤트를 전달했다. 다음 화면 확인에서 연두 강조와 행·열·숫자를 포함한 안내 문구가 보여야 한다.
- 보통 난이도 데모에서 힌트가 3회 남은 상태를 확인한 뒤 `H` 키를 네 번 전달했다. 다음 화면 확인에서 3회를 모두 사용한 뒤 버튼이 비활성화되고 한도 도달 문구가 표시되어야 한다.
- 보통 난이도의 한도 도달 화면에서 캔버스의 어려움 탭을 선택했다. 다음 화면에서 새 퍼즐과 `남은 힌트 2/2` 표시가 나타나면 난이도 전환 초기화가 확인된다.
- 브라우저 미리보기 이미지는 세로 크기가 축소되어 보이므로, 캔버스 포인터 검증에는 스크린샷의 시각 좌표가 아니라 실제 캔버스 1280×1100 기준 좌표를 사용한다.
- 실제 캔버스 좌표로 어려움 탭을 선택해 새 퍼즐과 `남은 힌트 2/2` 표시를 확인했다. 보통 난이도는 3/3에서 소진·비활성화도 확인했으며, 쉬움의 상수는 5회로 동일한 초기화 흐름을 따른다.
- 타임어택 데모에서 보통 난이도 7분 제한의 남은 시간 `02:31`을 시각 확인했고, 시간 종료 미리보기에서 `00:00`, TIME UP 오버레이, 다시 도전 버튼을 확인했다. 난이도별 시간 상태는 별도 스크립트로도 검증했다.
- 시간 종료 오버레이의 폭을 화면 너비의 86%까지 확장하고 제목을 가용 폭에 맞춰 축소했다. 데스크톱과 375px 모바일에서 `시간이 끝났습니다.` 문구가 패널 내부에 온전히 표시되는 것을 확인했다.
- 어려움 난이도에서 7개 이하의 값이 있는 셀을 코발트 물감 덩어리와 붓결로 5초간 가리는 이벤트를 추가했다. 데스크톱·375px 모바일 모두에서 효과와 안내 문구를 확인했으며, 자동 검증으로 쉬움 난이도 미적용 및 5초 후 해제를 확인했다.
- 375px 모바일에서는 보드를 화면 폭의 66%로 조정하고 도구 패널을 92% 폭으로 확장했다. 숫자 패드는 3열 기준 약 45px 높이, 지우기와 보조 버튼은 32~36px 높이로 확대되어 화면 안에서 읽기·터치가 쉬워졌다.
- 어려움 난이도의 타임어택을 5분에서 10분으로 조정했다. 어려움 미리보기에서 `09:56`과 `타임어택 10분` 표기를 시각적으로 확인했으며, 시간 종료 패널도 동일한 제한 시간을 참조한다.
- 타임어택을 쉬움 8분·보통 15분·어려움 20분으로 재조정했다. 자동 검증에서 각 난이도의 시작 초와 `08:00`·`15:00`·`20:00` 형식을 확인했고, 실제 어려움 브라우저 화면에서는 `19:58` 및 `타임어택 20분` 표기를 확인했다.
- `InputManager`는 문자 키값뿐 아니라 물리 키 코드도 단축키로 해석하도록 보완했다. macOS IME에서 `key`가 `ㅜ`처럼 변해도 `code: KeyN`이면 메모 모드로 전환되며, 키 반복은 이동 외 동작에 전달하지 않아 메모가 즉시 다시 꺼지는 것을 막는다. 자동 검증으로 N·새 게임·숫자 입력을 확인했다.
- 한글 입력 상태에서 브라우저가 N 키를 `ㅜ`로 보내면서 키 코드가 비어 있는 경로를 위해 `ㅜ → n` 별칭을 명시적으로 추가했다. 자동 검증에서 코드값 없는 `ㅜ`, 영문 N, 반복 입력 모두 메모 단축키 규칙을 따르는지 확인했다.
- 검증 기능은 숨은 정답과 비교하던 기존 동작을 제거하고 행·열·3×3 블록의 실제 중복만 빨간색으로 표시하도록 변경했다. 자동 검증에서 해답과 달라도 중복이 없는 후보 숫자는 오류 없이 유지되고, 실제 중복은 오류로 판정되는 것을 확인했다.
