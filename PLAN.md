# Game Plan: Sudoku Studio

## Risk Tasks

### 1. 유효한 스도쿠 퍼즐 생성과 난이도별 단서 수
- **Why isolated:** 유효한 해답을 유지하면서 빈칸 수를 조절해야 하며, 생성 중 행·열·3×3 블록 규칙이 무너지면 플레이 전체가 성립하지 않는다.
- **Approach:** 검증된 완성 해답을 기준으로 행·열 밴드와 숫자를 섞은 뒤, 난이도별 고정 개수의 칸을 비운다. 모든 입력·힌트·검증은 같은 해답 배열을 단일 기준으로 사용한다.
- **Verify:** 새 게임을 여러 번 시작해도 각 퍼즐의 주어진 숫자가 해답과 일치하고, 선택된 난이도에 따라 단서 수가 달라지며, 빈칸에 정답을 넣으면 완료 상태가 된다.

## Main Build

Babylon 기반의 2D 캔버스 게임 화면을 구축한다. 종이 질감의 넓은 지면 위에 9×9 스도쿠 보드를 왼쪽 중심으로 배치하고, 오른쪽 마진 노트에 난이도, 타이머, 진행도, 숫자 팔레트, 메모·힌트·검증·되돌리기 도구를 둔다. 키보드 숫자키/방향키/백스페이스와 포인터 선택을 모두 지원한다.

- **Assets needed:** 생성된 아이보리 종이 배경 텍스처, 코발트 인쇄 모티프, 3×3 브랜드 심볼, 게임 화면 시각 레퍼런스.
- **Verify:**
  - 셀을 선택하면 해당 행·열·3×3 블록과 같은 숫자가 구별된다.
  - 숫자 팔레트 및 키보드 입력은 빈칸에만 반영되고, 되돌리기는 마지막 입력을 되돌린다.
  - 메모 모드에서 여러 후보 숫자가 작은 조판 숫자로 나타나며, 일반 모드에서는 큰 숫자로 바뀐다.
  - 충돌 검증은 같은 행·열·블록의 중복을 즉시 드러내고, 힌트는 한 칸만 정답으로 채운다.
  - 타이머와 채움 진행도가 게임 중 갱신되고, 모든 빈칸이 정답이면 완료 패널이 나타난다.
  - 데스크톱과 모바일 화면에서 보드·도구·라벨이 겹치거나 잘리지 않는다.
  - 생성 자산이 누락되지 않고, 색상·규모·밀도는 `sudoku-studio-visual-target`의 에디토리얼 퍼즐 데스크 방향과 일치한다.
  - 캔버스 상호작용 중 브라우저 콘솔 오류가 없다.
  - `?demo`에서는 완성 직전의 결정적 보드 상태를 표시해 검증 스크린샷에서 실제 게임성을 확인할 수 있다.
